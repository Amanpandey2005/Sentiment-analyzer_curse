{
  "annotations": {
    "list": []
  },
  "editable": true,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0},
      "id": 1,
      "options": {"legend": {"displayMode": "list", "placement": "bottom"}},
      "targets": [
        {"expr": "sum(rate(sentiment_api_requests_total[5m])) by (endpoint)", "legendFormat": "{{endpoint}}"}
      ],
      "title": "Request Rate",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0},
      "id": 2,
      "targets": [
        {"expr": "histogram_quantile(0.95, sum(rate(sentiment_api_request_duration_seconds_bucket[5m])) by (le, endpoint))", "legendFormat": "p95 {{endpoint}}"}
      ],
      "title": "P95 API Latency",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 0},
      "id": 3,
      "targets": [
        {"expr": "sum(rate(sentiment_api_requests_total{http_status=~\"5..\"}[5m]))", "legendFormat": "5xx errors"}
      ],
      "title": "Error Rate",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 4,
      "targets": [
        {"expr": "sum(sentiment_predictions_total) by (sentiment)", "legendFormat": "{{sentiment}}"}
      ],
      "title": "Prediction Count by Sentiment",
      "type": "barchart"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "id": 5,
      "targets": [
        {"expr": "sum(rate(container_cpu_usage_seconds_total{name=~\".*api.*|.*dashboard.*\"}[5m])) by (name)", "legendFormat": "{{name}}"}
      ],
      "title": "Container CPU",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16},
      "id": 6,
      "targets": [
        {"expr": "container_memory_working_set_bytes{name=~\".*api.*|.*dashboard.*\"}", "legendFormat": "{{name}}"}
      ],
      "title": "Container Memory",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["mlops", "fastapi", "sentiment"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "ML Sentiment Platform",
  "uid": "ml-sentiment-platform",
  "version": 1
}
